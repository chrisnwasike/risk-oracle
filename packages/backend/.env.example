# ── Database ─────────────────────────────────────────────────────────────────
DATABASE_URL="postgresql://postgres:password@localhost:5432/risk_oracle"

# ── API server ────────────────────────────────────────────────────────────────
# PORT is optional — defaults to 3000 when not set
PORT=3000
NODE_ENV=development

# ── Blockchain ────────────────────────────────────────────────────────────────
# Required only for `npm run push` and `npm run query`.
# CONTRACT_ADDRESS must be 0x followed by exactly 40 hex characters.
# PRIVATE_KEY must be 0x followed by exactly 64 hex characters.
# Leave blank rather than using placeholder text — the server validates
# these formats and will error on startup if they don't match.
CONTRACT_ADDRESS=
PRIVATE_KEY=
RPC_URL=https://rpc.testnet.chain.robinhood.com
